# CFM static site — ensure HTML is served with correct Content-Type
[build]
  publish = "."
  # No build command; static HTML/JS/CSS

# Pretty URL: /search returns search.html (rewrite so browser gets HTML)
[[redirects]]
  from = "/search"
  to = "/search.html"
  status = 200
