// Copy this file to app-config.js and set your TinyMCE API key.
// app-config.js is gitignored so you can rotate the key without committing it.
// If app-config.js is missing, the app uses a fallback key in tinymce-loader.js (single place, easier to rotate than 10 HTML files).
window.TINYMCE_API_KEY = 'YOUR_TINYMCE_API_KEY';
